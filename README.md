a simple webstie check whether a given input ( number , text , special character) is a palindrome (a word, phrase, or sequence that reads the same backwards as forwards, e.g. madam or nurses run.)
or not :)
